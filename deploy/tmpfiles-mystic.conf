# Recreate the runtime directory after reboot so both crontabs can flock.
# Install: cp deploy/tmpfiles-mystic.conf /etc/tmpfiles.d/mystic.conf && systemd-tmpfiles --create
d /run/mystic 0775 mystic mystic -
