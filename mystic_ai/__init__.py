"""Mystic AI package: indicators and strategy implementations.

This package is additive and does not alter existing runtime behavior unless
explicitly enabled via environment gates in adapters/registry modules.
"""
